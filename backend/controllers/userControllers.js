const registerUser = (request, response) => {
    response.send('Register Route')
}

const loginUser = (request, response) => {
    response.send('Register Route')
}

modules.exports = {
    registerUser,
    loginUser,
}